/*
  Arduino requires a primary .ino file whose name matches the sketch folder.

  The LARP controller implementation remains in larp_scout_controller.ino,
  which Arduino automatically compiles as another tab in this same sketch.
*/
